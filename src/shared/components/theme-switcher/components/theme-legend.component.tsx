import * as React from 'react'

import {
	legend,
} from '../theme-switcher.module.scss'

const ThemeLegend: React.FunctionComponent = () => {
	return <div className={legend}>Color scheme</div>
}

export default ThemeLegend